<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tayyab.mobileapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileApp">
        <activity
            android:name=".activities.AuthActivity"
            android:exported="true"
            android:label="@string/title_activity_auth"
            android:theme="@style/Theme.MobileApp.NoActionBar" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainActivityShop"
            android:exported="true"
            android:label="@string/title_activity_main_shop"
            android:theme="@style/Theme.MobileApp.NoActionBar">

        </activity>
        <activity
            android:name=".MainActivitySC"
            android:exported="true"
            android:label="@string/title_activity_main_sc"></activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MobileApp.NoActionBar" />
    </application>

</manifest>